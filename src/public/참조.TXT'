map ((a, i) => {
    return (
        <div className = "food" key={i}>
        <Image src"" src={`/food${i}.png`} wclassName = "food-img" />
    )
})



'use client' --경우
1. 자바스크립트 사용 가능 .
onClick={()=>{}}

2. useState, useEffect 사용 가능 

serer componet 
로딩이 빠름.



전체적으로 서버 컴포넌트로 만들고,
부분적으로 자바 스크립트가 필요한 부분이 있으면 '클라이언트 컴포넌트'로 


let age = 20;
let name = 'kim'

export {age, name}

import {age, name} from './data.js'

{} 로 여러개의 변수를 넘길 수 잇음.


html 에서 {age} 사용 함.